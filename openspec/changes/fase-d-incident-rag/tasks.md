# Tasks: Fase D — Confirmed Incident Memory + Hybrid RAG (sparse-first)

Strict TDD is ACTIVE. Every impl task pairs a RED test with its GREEN impl. Two confirmation paths (operator + agent) feed `ConfirmedIncident`; sparse BM25 over pre-computed `searchTokens` ranks top-5 within a 90-day window, scoped per tenant. RRF plumbing (`RRF_K=60`) is ready for Phase 2 dense merge without schema change. `evidence.provenance.v1`, `shouldAbstain`, and `abstention.v1` are unchanged. Top-K=5, window=90d, permission=`view_network`, agent confirm gating=resolved ≥24h + no incomplete, pgvector deferred.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1700 (additions dominate; tests ≈ production) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | WU1 schema+contracts → WU2 BM25+retrieval → WU3 runtime+chat → WU4 confirm+admin+UI+E2E |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|------|------|-----------|----------------------|-----------------|-------------------|
| 1 — Schema+contracts | Prisma migration + zod envelopes + evidence re-exports | PR 1 | `pnpm --filter @ftth-copilot/{shared,db,evidence} test typecheck` | `pnpm turbo run test typecheck --filter=@ftth-copilot/{shared,db,evidence}` | Revert `schema.prisma` + drop migration + delete new contracts. Fase A/B/C untouched. |
| 2 — BM25+retrieval | `BM25Lite` + `retrieveRelevantIncidents` + `formatRelevantIncidentsBlock` + `buildPendingIncidentCandidate` | PR 2 | `pnpm --filter @ftth-copilot/evidence test` | `pnpm turbo run test typecheck --filter=@ftth-copilot/evidence` | Revert new files under `packages/evidence/src/`; revert index re-exports. Existing Fase B/C evidence tests stay green. |
| 3 — Runtime+chat | `retrievalProvider` option + pre-LLM block + chat route wiring + `PendingIncidentCandidate` write gate | PR 3 | `pnpm turbo run test typecheck --filter=@ftth-copilot/{agent-core,web}` | `pnpm turbo run test typecheck --filter=@ftth-copilot/{agent-core,web}` | Revert `runtime.ts` augmentation; revert `chat/route.ts` wiring (provider omitted → no-op). |
| 4 — Confirm+admin+UI+E2E | Operator confirm route + `IncidentsPanel` modal + admin promote route + Playwright E2E + workspace verify + README | PR 4 | `pnpm turbo run test typecheck` + `playwright test e2e/incidents-confirm.spec.ts` | `pnpm turbo run test typecheck` | Revert confirm/promote routes + modal + delete E2E. Pending candidates stay unread; manual promotion still works. |

## Phase D-1 — Schema + contracts

- [x] **D-1.1** RED→GREEN: add `ConfirmedIncident`, `PendingIncidentCandidate`, `ConfirmedBy` enum (`'operator'|'agent'|'system'`) to `packages/db/prisma/schema.prisma` with composite indexes `(tenantId, deviceKind, deviceId)`, `(tenantId, resolvedAt)`, `(tenantId, connectionId)`; create manual migration `packages/db/prisma/migrations/<ts>_confirmed_incidents/migration.sql` (style of `20260821080000_add_incidents/migration.sql`); add `confirmedIncidents`/`pendingIncidentCandidates` reverse relations on `Tenant`. Verify with `pnpm --filter @ftth-copilot/db typecheck`. Files: `packages/db/prisma/schema.prisma`, `packages/db/prisma/migrations/<ts>_confirmed_incidents/migration.sql`. Commit: `feat(db): add ConfirmedIncident + PendingIncidentCandidate + ConfirmedBy`.

- [x] **D-1.2** RED: `confirmedIncidentSchema` rejects `schema:'ftth.confirmed-incident.v2'`, rejects `score:1.5`, accepts base envelope; `pendingIncidentCandidateSchema` rejects bad status; both `.strict()`. GREEN: in `packages/shared/src/contracts.ts` add `CONFIRMED_INCIDENT_SCHEMA='ftth.confirmed-incident.v1' as const` + `PENDING_INCIDENT_CANDIDATE_SCHEMA='ftth.pending-incident-candidate.v1' as const`; add `confirmedIncidentSchema` (per design table incl. `score?` `0..1` and `searchTokens` non-empty) + `pendingIncidentCandidateSchema` (incl. `status: 'pending'|'promoted'|'rejected'`). Re-export from `packages/shared/src/index.ts` plus `RelevantIncidentResult = ConfirmedIncident & { score: number }`. Files: `packages/shared/src/contracts.ts`, `packages/shared/src/index.ts`, `packages/shared/tests/contracts.test.ts` (new). Commit: `feat(shared): add ftth.confirmed-incident.v1 + pending-incident-candidate contracts`.

- [x] **D-1.3** RED→GREEN: `packages/evidence/tests/index-exports.test.ts` asserts `ConfirmedIncident`, `PendingIncidentCandidate`, `RelevantIncidentResult` are re-exported from `@ftth-copilot/evidence` (type-only WU1 surface; runtime retrieval helpers land in WU2). Regenerated Prisma client. Files: `packages/evidence/src/index.ts`, `packages/evidence/tests/index-exports.test.ts`. Commit: `chore(monorepo): regenerate Prisma client for ConfirmedIncident models`.

## Phase D-2 — BM25 + retrieval

- [x] **D-2.1** RED: stop-word drop (`["la","onu","caída"]`→`["onu","caída"]`); empty intersection returns `0`; `scoreBM25` is symmetric in (q,d) for identical intersection; 4-doc golden corpus at `packages/evidence/tests/fixtures/bm25-golden.json` for query `["rx","bajo"]` ranks docs in fixed order. GREEN: create `packages/evidence/src/bm25-lite.ts` exporting `scoreBM25(queryTokens, docTokens, k1=1.5, b=0.75)`, `tokenize(text)`, `TOKEN_REGEX=/^[a-záéíóúñ0-9]+$/`, `BM25_STOPWORDS` (locked 30-word trimmed set per design), `BM25_STOPWORDS_FULL` (~80-word set for Phase 2). Pure TS, no Prisma dep. Files: `packages/evidence/src/bm25-lite.ts`, `packages/evidence/tests/bm25-lite.test.ts` (new), `packages/evidence/tests/fixtures/bm25-golden.json` (new). Commit: `feat(evidence): BM25Lite pure-TS scorer + locked Spanish stop-words`.

- [x] **D-2.2** RED: empty `tenantId` throws `MissingTenantError`; `mode:'demo'` short-circuits to `[]` even when rows match; row at `now-100d` excluded, row at `now-30d` returned (`sinceDays:90`); 10-row corpus returns ≤5 with every returned `rrfScore ≥ 0.05`; two equal-BM25 rows → deviceHint-matched row ranks higher; cross-tenant read with rows for `t1` queried as `t2` returns `[]`; cold start (zero rows) returns `[]`. GREEN: create `packages/evidence/src/relevant-incidents.ts` exporting `retrieveRelevantIncidents({tenantId,query,deviceHint?,limit=5,sinceDays=90,mode='live',now?})`, `MissingTenantError`, `RRF_K=60`, `MIN_SPARSESCORE=0.05`, `DEFAULT_LIMIT=5`, `DEFAULT_SINCE_DAYS=90`. Uses `vi.mock('@ftth-copilot/db')` pattern (same as `apps/web/tests/api/chat-abstention.test.ts:43`). Files: `packages/evidence/src/relevant-incidents.ts`, `packages/evidence/tests/relevant-incidents.test.ts` (new). Commit: `feat(evidence): retrieveRelevantIncidents + RRF plumbing + tenant refusal`.

- [x] **D-2.3** RED: `RELEVANT_INCIDENTS_HEADING` is byte-identical to the design literal (incl. "contexto, no evidencia" marker) and byte-identical across invocations; `formatRelevantIncidentsBlock([])` returns `''`; with 2 incidents the output is byte-identical to the design template `"[N] YYYY-MM-DD — {deviceId} {summary}. Causa raíz: {rootCause}. Fix: {fix}. Score: {score.toFixed(2)}\n"` (N is 1-indexed), `score` formatted to 2 decimals, dates rendered as observedAt's YYYY-MM-DD. GREEN: append `formatRelevantIncidentsBlock(incidents: RelevantIncidentResult[]): string` + `RELEVANT_INCIDENTS_HEADING` constant to `packages/evidence/src/relevant-incidents.ts`. Files: `packages/evidence/src/relevant-incidents.ts`, `packages/evidence/tests/relevant-incidents.test.ts` (extend). Commit: `feat(evidence): Spanish RELEVANT_INCIDENTS_HEADING + formatRelevantIncidentsBlock (snapshot-locked)`.

- [x] **D-2.4** RED: `buildPendingIncidentCandidate({tenantId,summary,toolCallsJson,runSessionId?,sourceIncidentId?})` returns shape with `schema:'ftth.pending-incident-candidate.v1'`, `status:'pending'` default, `proposedConfirmedAt` defaulted to `now` ISO; throws on empty `tenantId`/`summary`. GREEN: create `packages/evidence/src/pending-incident.ts` exporting `buildPendingIncidentCandidate(args)`. Pure constructor (no Prisma). Files: `packages/evidence/src/pending-incident.ts`, `packages/evidence/tests/pending-incident.test.ts` (new). Re-export from `packages/evidence/src/index.ts`. Commit: `feat(evidence): buildPendingIncidentCandidate helper`.

## Phase D-3 — runAgent + chat route wiring

- [x] **D-3.1** RED: 3 scenarios — `mode:'live'` + provider returning 2 incidents → `system` arg of `llm.createMessage` contains `RELEVANT_INCIDENTS_HEADING` + 2 formatted lines; `mode:'demo'` (any provider) → no heading; provider returns `[]` or `undefined` → no heading. Plus Truth-Gate invariant: with 2 retrieved + 2 tool calls, `result.verdicts.length===2` AND `result.toolCalls` contains no retrieved rows. GREEN: in `packages/agent-core/src/runtime.ts` add `retrievalProvider?: (args:{tenantId:string;query:string}) => Promise<RelevantIncidentResult[]>` to `RunAgentOptions`; between `sourcePrompt` construction (L173) and the `for` loop (L179), if `opts.dataSource?.mode==='live'` AND `opts.retrievalProvider` AND `results.length>0`, prepend `RELEVANT_INCIDENTS_HEADING + formatRelevantIncidentsBlock(results)` to the LLM `system`. Import `RelevantIncidentResult, RELEVANT_INCIDENTS_HEADING, formatRelevantIncidentsBlock` from `@ftth-copilot/evidence`. Re-export `RunAgentOptions` is unchanged. Files: `packages/agent-core/src/runtime.ts`, `packages/agent-core/tests/runtime.test.ts` (extend). Commit: `feat(agent-core): retrievalProvider injection + pre-LLM confirmed-incident block`.

- [x] **D-3.2** RED: 4 scenarios in `apps/web/tests/api/chat-rag.test.ts` — clean+live+no incomplete → exactly one `PendingIncidentCandidate` row + `runAgent` was called with `retrievalProvider` AND `system` contains heading; abstained (`result.abstained:true`) → zero candidates; any verdict `code:'incomplete'` → zero candidates; `mode:'demo'` → zero candidates + `retrievalProvider` still wired (gating inside the function). GREEN: in `apps/web/app/api/chat/route.ts` after `resolveTenantConnector` (L116), call `retrieveRelevantIncidents({tenantId:user.tenantId, query:message, deviceHint, mode:resolved.dataSource.mode, limit:5, sinceDays:90})`; pass `retrievalProvider: async () => results` to `runAgent`. After `prisma.message.create({role:'assistant'})` (L202), when `result.abstained !== true` AND `!result.verdicts.some(v=>v.code==='incomplete')` AND `resolved.dataSource.mode==='live'`, write exactly one `prisma.pendingIncidentCandidate.create({tenantId:user.tenantId, runSessionId, summary, toolCallsJson, sourceIncidentId, proposedConfirmedAt:new Date(), status:'pending'})`. Mock `retrieveRelevantIncidents` via `vi.mock('@ftth-copilot/evidence')`. Files: `apps/web/app/api/chat/route.ts`, `apps/web/tests/api/chat-rag.test.ts` (new). Commit: `feat(web): chat route wires retrievalProvider + PendingIncidentCandidate write gate`.

## Phase D-4 — Operator confirm route + UI

- [ ] **D-4.1** RED: 6 scenarios — `!hasPermission('view_network')` → `403` zero writes; `{rootCause:''}` → `400`; unknown incident id → `404`; `status:'open'` → `409`; happy path (resolved + valid body + permission) → `200` + 1 `ConfirmedIncident` (`confirmedBy:'operator',confirmedByUserId:user.id`) + 1 `AgentActionLog` (`toolName:'__operator_confirm__'`); idempotent re-POST → `200` returning existing row, zero new writes. GREEN: create `apps/web/app/api/incidents/[id]/confirm/route.ts` exporting `POST` (Node runtime, dynamic). Body zod: `{rootCause:z.string().min(1),fix:z.string().min(1),summary:z.string().min(1)}`. Look up `prisma.incident.findFirst({where:{id,tenantId:user.tenantId}})`; `prisma.confirmedIncident.findFirst({where:{tenantId,sourceIncidentId:id}})` for idempotency; write `ConfirmedIncident` (tokenize summary/rootCause/fix into `searchTokens` lowercased) + `AgentActionLog` (`toolName:'__operator_confirm__', parameters:body, result:confirmedIncident.id`). Files: `apps/web/app/api/incidents/[id]/confirm/route.ts` (new), `apps/web/tests/api/incidents-confirm.test.ts` (new). Commit: `feat(web): operator confirm route with view_network gate + idempotency`.

- [ ] **D-4.2** RED→GREEN: in `apps/web/components/IncidentsPanel.tsx`, add "Marcar como confirmado" button on each `incident.status==='resolved'` row, gated on `auth.user.role` having `view_network`. Click opens a modal with three required textareas (`rootCause`, `fix`, `summary`); submit POSTs to `/api/incidents/${id}/confirm`; on `200` show success toast + refetch list; on `403/404` show inline error. Button hidden when fetch silently fails (feature-flag pattern per design). Files: `apps/web/components/IncidentsPanel.tsx`, `apps/web/tests/components/IncidentsPanel.test.tsx` (new, vitest+testing-library if present; else skip and rely on E2E in D-6.2). Commit: `feat(web): IncidentsPanel "Marcar como confirmado" action + modal`.

## Phase D-5 — Admin promotion route

- [ ] **D-5.1** RED: 2 scenarios — candidate whose `Incident.resolvedAt = now-12h` → zero promotions; candidate with `Incident.status='resolved' for ≥24h` + no `incomplete` verdict in stored `toolCallsJson` → 1 `ConfirmedIncident` (`confirmedBy:'agent'`) + 1 `AgentActionLog` (`toolName:'__agent_promote__'`); still-open `Incident` → rejected; non-OWNER role → `403`. GREEN: in `packages/evidence/src/pending-incident.ts` add `promotePendingIncidents(now: Date): Promise<{promoted: number}>` (iterates `pendingIncidentCandidate` × `incident` joined, filters `Incident.status==='resolved'` AND `Incident.resolvedAt <= now-24h` AND no `incomplete` in `toolCallsJson`, writes both rows, updates candidate `status='promoted'`). In `apps/web/lib/auth/permissions.ts` document `OWNER` as the only admin promoter (no new permission). Create `apps/web/app/api/pending-incidents/promote/route.ts` exporting `POST`, OWNER-only gate, calls the helper. Files: `packages/evidence/src/pending-incident.ts` (extend), `packages/evidence/tests/pending-incident.test.ts` (extend), `apps/web/lib/auth/permissions.ts` (comment-only), `apps/web/app/api/pending-incidents/promote/route.ts` (new), `apps/web/tests/api/pending-incidents-promote.test.ts` (new). Commit: `feat(web): admin promotion route + promotePendingIncidents helper`.

## Phase D-6 — Snapshot + E2E

- [ ] **D-6.1** RED: `RELEVANT_INCIDENTS_HEADING` byte-equality against inline literal at `packages/evidence/tests/fixtures/relevant-incidents-heading.golden.txt`; `formatRelevantIncidentsBlock` 2-incident input byte-equality against `packages/evidence/tests/fixtures/relevant-incidents-block-2.golden.txt`. GREEN: commit the two golden files as fixtures; lock the snapshot bytes; assert in the existing `relevant-incidents.test.ts`. Files: `packages/evidence/tests/fixtures/relevant-incidents-heading.golden.txt` (new), `packages/evidence/tests/fixtures/relevant-incidents-block-2.golden.txt` (new), `packages/evidence/tests/relevant-incidents.test.ts` (extend). Commit: `test(evidence): snapshot-lock RELEVANT_INCIDENTS_HEADING + formatRelevantIncidentsBlock`.

- [ ] **D-6.2** RED→GREEN: `apps/web/e2e/incidents-confirm.spec.ts` — mock `/api/auth/me` (OWNER), `/api/incidents` (one resolved incident), `/api/incidents/:id/confirm` (200); navigate to `/app`, locate resolved incident card, click "Marcar como confirmado", fill the three fields, submit, assert success state. Mirror `apps/web/e2e/chat-abstention.spec.ts` pattern (`page.route` mocks; no Postgres). Files: `apps/web/e2e/incidents-confirm.spec.ts` (new). Commit: `test(web): Playwright E2E for operator confirm modal`.

## Phase D-7 — Workspace regression sweep

- [ ] **D-7.1** GREEN: run `pnpm turbo run test typecheck` workspace-wide; confirm zero failures. Spot-run the Fase A/B/C golden suites manually (`packages/evidence/tests/{truth-gate,abstention-policy,index-exports}.test.ts`, `packages/agent-core/tests/{runtime,provenance}.test.ts`, `apps/web/tests/api/chat-abstention.test.ts`). All pre-Fase-D tests must pass unmodified. Files: none. Commit: `chore(monorepo): verify turbo run test typecheck green with Fase D`.

- [ ] **D-7.2** GREEN: in `packages/evidence/README.md` add a Fase D section — `RELEVANT_INCIDENTS_HEADING` literal, `formatRelevantIncidentsBlock` line format, `retrieveRelevantIncidents` args contract, `RRF_K=60` plumbing for Phase 2 dense merge, `promotePendingIncidents(now)` helper, rollback via `retrievalProvider` being optional (omit → no-op) + dropping the two tables in down migration. Files: `packages/evidence/README.md`. Commit: `docs(evidence): document Fase D retrieval + promotion API + rollback`.

## Strict TDD Reminder

Per `openspec/config.yaml` `strict_tdd: true`, every implementation task above has a paired RED test description in its body. Apply agents run the RED step first (commit failing test), then the GREEN step (commit passing impl), then a small REFACTOR if natural. Tests must accompany the production change in the same work-unit commit so reviewers see the contract and the proof together.
