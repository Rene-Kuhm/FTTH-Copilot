// Phase F-2: skeleton only. Phase F-4 adds the runner / assertions / metrics modules.
//
// The barrel is intentionally empty at this stage so that downstream
// consumers can `@ftth-copilot/eval` today and import future exports as
// they land, without breaking the workspace wiring. See
// `openspec/changes/fase-f-eval-injection/specs/eval-harness/spec.md` for
// the contract this barrel will eventually re-export.
export {};
